--- #jenkins_ansible_tomcat
 - hosts: tomcat
   tasks:
           - name: deploying war into tomcat server
             copy:
                     src: /home/vagrant/playbooks/mywar/addressbook.war
                     dest: home/vagrant/var/lib/tomcat9/webapps
                     remote_src: false
             become: yes
    
         
