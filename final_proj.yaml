--- #jenkins_ansible_tomcat
 - hosts: tomcat
   become: yes
   tasks:
           - name: deploying war into tomcat server
             copy:
                     src: /home/vagrant/ansible/workspace/project_pipeline_1/project1_package_phase/target/addressbook.war
                     dest: home/vagrant/myjar
